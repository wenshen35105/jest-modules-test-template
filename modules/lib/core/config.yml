platform:
  schema: https
  host: google.ca
  port: 443

auth:
  schema: https
  host: youtube.ca
  port: 443

selenium:
  browser: chrome
  webDriverCycle: test # when webDriver should be restarted? In "run" / "test" scope
